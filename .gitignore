# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Project data and models (Tracked by DVC, NOT Git)
/data/raw/server_metrics.csv
/data/processed/
/models/

# Note: /metrics/ is NOT ignored here because DVC is configured with `cache: false` 
# for metrics.json and plots, meaning Git should track them natively.
